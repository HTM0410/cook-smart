train:
  base_model: yolo11n.pt
  epochs: 50
  imgsz: 640
  batch: 16
  patience: 15
  optimizer: AdamW
  lr0: 0.001
  seed: 42
  deterministic: true
  workers: 4
  device: "0"
  project: ingredient-detection
  wandb: true

evaluate:
  batch: 16
  min_map50: 0.50
  min_map50_95: 0.30
