perf(cache): cache recipe list endpoint + invalidate on writes

- Add CACHE_KEYS.RECIPE_LIST / CACHE_TTL.RECIPE_LIST (10min)
- Add cacheService.getCachedRecipeList / cacheRecipeList / invalidateRecipeListCaches
- Update invalidateRecipeCaches to also DEL recipes:list:*
- Wrap getAllRecipes with cache (key on p/l/s/status/sort/order)
- Invalidate recipe list caches on create/update/delete/toggle/addStep/addIngredient

Target: fix /api/recipes cold-start latency (~700ms -> ~5-50ms when hit)
