ci(yolo): fix env.YOLO_SOURCE_DIR default so push trigger builds work

github.event.inputs.source_dir is null on push events, so the build
context resolved to an empty string and Docker build-push-action failed
immediately. Move the default into env so both triggers work.